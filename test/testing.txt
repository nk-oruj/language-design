
(* this is my module declaration *)
(* i am testing the comments too overall *)
module MyModule

(* this is a dummy procedure to test *)
(* the algorithmic implementation of fibonoachi *)
procedure TestingFibonachi
record
    index gets [int]
    value sets [int]

    elementA [int]
    elementB [int]
    elementC [int]

scheme

    load elementA with 0
    load elementB with 1

    scheme loop
        
        load elementC with (add elementA elementB)
        load elementA with (elementB)
        load elementB with (elementC)
        
        drop loop if (le index 0)
        load index with (sub index 1)
        lift loop

    finish loop

    load value with (elementB)

finish

finish MyModule
